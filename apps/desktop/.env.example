# Voquill Desktop - Environment Variables Example
# Copy this file to .env.local and fill in your values

# =============================================================================
# CRITICAL - Required for Production
# =============================================================================

# Secret key for API key encryption (ChaCha20-Poly1305)
# Generate a strong random string (32+ characters recommended)
# WARNING: Changing this will invalidate all existing encrypted API keys!
VOQUILL_API_KEY_SECRET=your-secret-key-here-change-this-in-production

# =============================================================================
# OAuth - Optional (for cloud features)
# =============================================================================

# Google OAuth credentials (required for cloud sync/login)
# Get these from: https://console.cloud.google.com/apis/credentials
VOQUILL_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VOQUILL_GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# Development / Testing
# =============================================================================

# Override platform detection (useful for cross-platform testing)
# Values: darwin, win32, linux
# VOQUILL_DESKTOP_PLATFORM=win32

# Force CPU-only Whisper inference (disable GPU)
# Set to 1 to disable GPU acceleration
# VOQUILL_WHISPER_DISABLE_GPU=1

# Environment flavor
# Values: dev, prod, emulators
# VITE_FLAVOR=emulators

# Firebase configuration (for emulators/development)
# VITE_USE_EMULATORS=true
# VITE_FIREBASE_API_KEY=your-firebase-api-key
# VITE_FIREBASE_AUTH_DOMAIN=your-app.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id

# Dev server host override
# TAURI_DEV_HOST=localhost

# =============================================================================
# Internal (Auto-configured - DO NOT SET MANUALLY)
# =============================================================================

# These are set automatically by the application
# VOQUILL_KEYBOARD_LISTENER=1
# VOQUILL_KEYBOARD_PORT=<auto-assigned-port>

# =============================================================================
# Debug Flags (Development only)
# =============================================================================

# Enable debug logging for keyboard events
# VOQUILL_DEBUG_KEYS=1

# Enable debug logging for paste operations
# VOQUILL_DEBUG_PASTE_TEXT=1

# =============================================================================
# Notes
# =============================================================================

# 1. VOQUILL_API_KEY_SECRET is CRITICAL for security
#    - Use a strong, random value
#    - Never commit to version control
#    - Keep backups if you need to preserve encrypted keys
#
# 2. OAuth credentials are only needed for cloud features
#    - App works fully offline without them
#    - Required for Firebase authentication
#
# 3. Platform override is useful for testing
#    - Normally auto-detected
#    - Override only when needed
#
# 4. GPU can be disabled for debugging
#    - Useful if GPU drivers cause issues
#    - CPU inference is slower but more compatible
